#ifndef RNAK_H
#define RANK_H

/*
int exists_in_ranking(const char* name)
{
    for (int i = 0; i < ranking_count; i++) {
        if (strcmp(rankings[i].name, name) == 0) {
            return 1;
        }
    }
    return 0;
}
*/
#endif // !
